/* This file was generated by SableCC's ObjectMacro. */

package org.sablecc.objectmacro.macro;

import java.util.LinkedList;
import java.util.List;

public class Macro_append {

    // parameter declarations
    private final String param_indent;

    // nested macro declarations
    private final List<Macro_instruction> macro_instruction_list = new LinkedList<Macro_instruction>();

    // constructor
    Macro_append(
            String param_indent) {

        this.param_indent = param_indent;
    }

    // toString
    @Override
    public String toString() {

        StringBuilder sb = new StringBuilder();
        sb.append(this.param_indent);
        sb.append("  public void appendTo(StringBuilder sb) {");
        sb.append(System.getProperty("line.separator"));
        for (Macro_instruction macro_instruction : this.macro_instruction_list) {
            sb.append(macro_instruction.toString());
        }
        sb.append(this.param_indent);
        sb.append("  }");
        sb.append(System.getProperty("line.separator"));
        return sb.toString();
    }

    // nested macros
    public Macro_instruction newMacro_instruction() {

        Macro_instruction macro_instruction = new Macro_instruction(
                this.param_indent);
        this.macro_instruction_list.add(macro_instruction);
        return macro_instruction;
    }

}
