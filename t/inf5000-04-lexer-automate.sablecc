Grammar automate:
Lexer
  letter = 'a'..'z';
  digit = '0'..'9';
  id = letter(letter|digit)*;
  if = 'if';
  int = (digit)+;
  float = (digit)+ '.' (digit)+;
  dot = '.';
  bl = ' ';
  Token id, if, int, float, dot;
  Ignored bl, #10, #13;
