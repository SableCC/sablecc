<?xml version="1.0"?>
<!--
  This file is part of SableCC ( http://sablecc.org ).

  See the NOTICE file distributed with this work for copyright information.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.                                                                    
-->

<project name="SableCC" default="all" basedir="..">

  <!-- Global Properties -->
  <property name="package" value="SableCC" />
  <property name="src.dir" value="src" />
  <property name="classes.dir" value="classes" />
  <property name="lib.dir" value="lib" />
  <property name="junit-tests.dir" value="junit-tests" />
  <property name="regression-tests.dir" value="regression-tests" />
  <property name="doc.dir" value="doc" />
  <property name="devel-doc.dir" value="devel-doc" />
  <property name="sablecc.version" value="repository" />
  <property name="dist.dir" value="sablecc-${sablecc.version}" />
  <property name="dist.file" value="sablecc-${sablecc.version}.zip" />

  <!-- Paths -->
  <path id="classpath.base" />

  <path id="classpath.test">
    <pathelement location="${classes.dir}" />
    <path refid="classpath.base" />
  </path>

  <!-- Default Target -->
  <target name="all"
          depends="init, compile, jar, tests, doc"
          description="Default target. Executing all build targets" />

  <!-- Initiation Stage -->
  <target name="init" description="Initiating build.">
    <condition property="ant-good-version">
      <antversion atleast="1.7.0" />
    </condition>
  </target>

  <!-- Compiling Stage -->
  <target name="compile"
          depends="compile-src, compile-tests"
          description="Compiling project files." />

  <target name="compile-src"
          depends="init"
          description="Compiling source files.">

    <mkdir dir="${classes.dir}" />

    <javac srcdir="${src.dir}"
           destdir="${classes.dir}"
           includes="**/*.java"
           listfiles="yes" />

  </target>

  <target name="compile-tests"
          depends="jar"
          description="Compiling JUnit and regression tests.">

    <javac srcdir="${junit-tests.dir}" destdir="${classes.dir}" />

    <javac srcdir="${regression-tests.dir}" destdir="${classes.dir}" />

  </target>

  <!-- Packaging Stage -->
  <target name="jar" depends="compile-src" description="Creating jar file.">

    <mkdir dir="${lib.dir}" />

    <jar destfile="${lib.dir}/sablecc-${sablecc.version}.jar"
         basedir="${classes.dir}" />

  </target>

  <!-- Testing Stage -->
  <target name="tests" depends="tests-failed" if="ant-good-version" />

  <target name="tests-execute"
          depends="compile-tests"
          description="Running JUnit and regression tests.">

    <junit failureProperty="test.failure">
      <classpath refid="classpath.test" />
      <formatter type="brief" usefile="false" />
      <batchtest>
        <fileset dir="${classes.dir}" includes="**/*Test.class" />
        <fileset dir="${classes.dir}" includes="**/*Bug.class" />
      </batchtest>
    </junit>

  </target>

  <target name="tests-failed" depends="tests-execute" if="test.failure">
    <echo message="perform test cleanup" />
    <!-- still to implement-->
    <fail message="test failed" />
  </target>

  <!-- JavaDoc Stage -->
  <target name="doc" description="Generate JavaDoc">

    <mkdir dir="${doc.dir}" />

    <javadoc destdir="${doc.dir}"
             author="true"
             use="true"
             windowtitle="SableCC-4"
             source="1.6">
      <packageset dir="${src.dir}" />
      <link href="http://java.sun.com/javase/6/docs/api/" />
    </javadoc>

    <javadoc destdir="${devel-doc.dir}"
             access="private"
             author="true"
             use="true"
             windowtitle="SableCC-4"
             source="1.6">
      <packageset dir="${src.dir}" />
      <link href="http://java.sun.com/javase/6/docs/api/" />
    </javadoc>

  </target>

  <!-- Cleaning Stage -->
  <target name="clean-all" description="Deleting doc,bin and lib directories">
    <delete dir="${classes.dir}" />
    <delete dir="${lib.dir}" />
    <delete dir="${doc.dir}" />
    <delete dir="${devel-doc.dir}" />
    <delete file="${dist.file}" />
  </target>

  <!-- Distribution Stage -->
  <target name="dist"
          depends="all"
          description="Creating distribution zip file.">

    <mkdir dir="${dist.dir}" />

    <copy file="README" todir="${dist.dir}" />
    <copy file="LICENSE" todir="${dist.dir}" />
    <copy file="NOTICE" todir="${dist.dir}" />

    <copy todir="${dist.dir}">
      <fileset dir="." includes="${lib.dir}/**" />
      <fileset dir="." includes="${doc.dir}/**" />
      <fileset dir="." includes="${src.dir}/**" />
      <fileset dir="." includes="ant-files/**" />
      <fileset dir="." includes="${junit-tests.dir}/**" />
      <fileset dir="." includes="${regression-tests.dir}/**" />
    </copy>

    <zip destfile="${dist.file}" basedir="." includes="${dist.dir}/**" />

    <delete dir="${dist.dir}" />

  </target>

</project>
